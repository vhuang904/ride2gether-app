{
  "name": "ride2gether-app",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:e2e": "playwright test",
    "test:rules": "firebase emulators:exec --project demo-ride2gether --only firestore \"node --test tests/rules/*.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@playwright/test": "^1.56.0",
    "firebase": "^11.10.0",
    "firebase-tools": "^14.15.0"
  }
}
